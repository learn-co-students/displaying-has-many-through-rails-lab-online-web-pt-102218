<%= @doctor.appointments.each do |i| %>
    <%= "#{i.appointment_datetime.strftime("%B %e, %Y")} at #{i.appointment_datetime.strftime("%H:%M")}" %>
    <%= link_to i.patient.name, patient_path(i.patient) %>
<% end %> 