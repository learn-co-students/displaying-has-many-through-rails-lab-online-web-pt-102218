<h2><%= @appointment.appointment_datetime.strftime("%B %e, %Y at %H:%M %p") %></h2>
<%= link_to @appointment.patient.name, patient_path(@appointment.patient_id) %>
<%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor_id) %>