<% @patient.appointments.each do |appointment| %> 
    <%= appointment.appointment_datetime.to_datetime.strftime("%B %d, %Y at %H:%M") %> 
<%end%>
<% @patient.doctors.each do |doctor| %> 
    <%= link_to doctor.name, doctor_path(doctor) %> 
<%end%> 