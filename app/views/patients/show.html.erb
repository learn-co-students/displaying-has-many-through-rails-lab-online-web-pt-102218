<h1> <%= @patient.name %> Appointment Times </h1>

    <p> Doctor: <%= link_to @patient.doctors.first.name, doctor_path(@patient.doctors.first.id) %> , Appointment:<%= @patient.appointments.first.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> </p>
