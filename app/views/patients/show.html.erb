<%= @patient.name%><br>
Appointments:<br>
<% @patient.appointments.each do |appt|%>
    <%= link_to appt.doctor.name, doctor_path(appt.doctor.id)%><%= appt.appointment_datetime.strftime("%B %e, %Y at %k:%M") %>
<% end %>